# definisco le variabili di ambiente che posso utilizzare globalmente nell'app
# per fare in modo che node sia in grado di utilizzare questi file .env devo installare il package dotenv
# npm i dotenv
# Dotenv is a zero-dependency module that loads environment variables from a .env file into process.env
NODE_ENV=development
PORT_LISTEN=7777